---
name: flexilink
---

FlexiLinkField:

  # field types to allow in the dropdown selection
  # leave empty to allow all defined types 
  allowed_types:

  # field type definitions
  field_types:
    Page:
      field: TreeDropdownField
      description: Select a Page to Link To
    ExternalURL: 
      field: TextField
      description: URL to link to (include http:// ...)
    YouTubeID:
      field: TextField
      description: YouTube Video ID
